<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      BLE permissions span two eras, and minSdk 26 means both ship.

      API 31+: BLUETOOTH_SCAN / BLUETOOTH_CONNECT. neverForLocation is set on
      the scan permission because this app does not derive location from BLE
      results, which spares the user a location prompt.

      Below 31: the legacy pair, plus ACCESS_FINE_LOCATION. Without that
      permission the platform silently returns zero scan results, which is the
      classic way a BLE app appears broken while looking correct.

      Since M7, location is also requested for its own sake, on every API level:
      the nearby ranking compares the phone's position against places the user
      tagged by hand. That is why ACCESS_FINE_LOCATION no longer carries
      maxSdkVersion 30. Capped, it was granted only on old phones, and the
      ranking would have failed silently on every modern one.

      Foreground only. There is deliberately no ACCESS_BACKGROUND_LOCATION and
      no geofence registration: a position is read while someone is looking at
      the app, and never otherwise.
    -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
      The emulation runs on the Flipper, but the link to it lives in this
      process, and a backgrounded process is one the system may reclaim. A
      foreground service of type connectedDevice is the platform's answer, and
      it brings the notification the situation deserves anyway: something is
      being emitted on your behalf, with a visible way to stop it.

      POST_NOTIFICATIONS is requested at runtime on API 33+. Without it the
      service still runs, but its notification, and with it the Stop action,
      is silently suppressed.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application
        android:name=".OmniWalletApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.OmniWallet">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.OmniWallet">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".session.EmulationService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- The cards you are most likely to want, one tap from the home screen. -->
        <receiver
            android:name=".quick.WalletWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/wallet_widget_info" />
        </receiver>

        <service
            android:name=".quick.EmulationTileService"
            android:exported="true"
            android:icon="@drawable/ic_notification"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!--
          Exported on purpose, and fenced accordingly. Anything on the phone
          that can send a broadcast can reach this, which is what makes it
          useful to Tasker and what makes it a liability. It is off by default,
          refuses while the app lock is on, and records every call so Settings
          can show what last triggered it. See AutomationReceiver.
        -->
        <receiver
            android:name=".quick.AutomationReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="dev.omniwallet.action.EMULATE" />
                <action android:name="dev.omniwallet.action.STOP" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
